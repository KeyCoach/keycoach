import Image from "next/image";

export function Link({
    href,
    children,
    className,
    }: Readonly<{
    href: string;
    children: React.ReactNode;
    className?: string;
    }>) {
    return (
        <a></a>
    );
}
